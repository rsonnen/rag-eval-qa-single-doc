# Single-document question generator configuration
#
# All models accessed via LiteLLM proxy.

generator_model:
  name: cloud-medium
  kwargs:
    reasoning_effort: medium

validator_model:
  name: cloud-small
  kwargs:
    reasoning_effort: none

deduplicator_model:
  name: cloud-small
  kwargs:
    reasoning_effort: none
